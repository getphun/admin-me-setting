<?php $this->partial('shared/header'); ?>

<div class="container">
    <div class="row">
        <div class="col-md-3">
            <?php $this->partial('me/setting/sidebar'); ?>
        </div>
        <form method="post" class="col-md-9">
            <div class="page-header">
                <button class="btn btn-primary pull-right">Save</button>
                <h3>Password</h3>
            </div>
            
            <?php if($saved): ?>
            <div class="alert alert-success">
                <strong>Success!</strong> Your new password is set.
            </div>
            <?php endif; ?>
            
            <div class="row">
                <div class="col-md-6">
                    <?= $this->form->field('current_password'); ?>
                    <hr>
                    <?= $this->form->field('new_password'); ?>
                    <?= $this->form->field('retype_password'); ?>
                    <hr>
                    <?= $this->form->field('truncate_session'); ?>
                </div>
            </div>
        </form>
    </div>
</div>

<?php $this->partial('shared/footer'); ?>